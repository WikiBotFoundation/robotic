processor;
 soruce.id( = whiteassembly
 processor; chpu.data
   ( < STILL; cphu
 collection= { istring.git
   ( < dev= ( (id
 langu.id
        print(cphu)
      (> cphu.transis
      transis = <string.tn
         print(transis
     CreateTransis { 2+6_ float.id
   (< Cphu.file
       star.fpp
           print(machine.id)
      (> processor.id
          Float.id ( = { processor.name
      (> coding. { ) Float
          ts.assemblty
